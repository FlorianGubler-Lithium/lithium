apiVersion: apps/v1
kind: Deployment
metadata:
  name: floriangubler-ch-deployment
spec:
  selector:
    matchLabels:
      app: floriangubler-ch
  replicas: 2
  template:
    metadata:
      labels:
        app: floriangubler-ch
    spec:
      containers:
        - name: floriangubler-ch
          image: florianzh/floriangubler-ch:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: web-service
spec:
  selector:
    app: floriangubler-ch
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080